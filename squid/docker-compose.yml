version: '3.6'
services:
  squid:
    image: sameersbn/squid
    volumes:
      - cache:/var/spool/squid
      - ./squid.conf:/etc/squid/squid.conf
    ports:
      - 8118:3128
    restart: always
volumes:
  cache: